#=================================================================#
#                Contoh File Konfigurasi Lingkungan               #
#=================================================================#
#
# Salin file ini ke .env dan sesuaikan nilainya jika perlu.
# Nilai-nilai ini akan digunakan oleh docker-compose.yml untuk
# mengkonfigurasi semua layanan (database, auth, product, dll).
#
# JANGAN PERNAH memasukkan file .env ke dalam version control (Git).
#

# --- Konfigurasi Database ---
# Variabel ini digunakan oleh layanan 'db' dan layanan aplikasi lainnya.

# Host database. 'db' adalah nama service di docker-compose.
# Local development env
DB_HOST=127.0.0.1

# Port database yang diekspos ke host machine.
DB_PORT=3307

# User database.
DB_USER=user

# Password untuk user database.
DB_PASS=password

# Nama database yang akan dibuat.
DB_NAME=ecommerce

# --- Konfigurasi Keamanan ---
JWT_SECRET=supersecretjwtkey

# Service ports when running locally (optional)
AUTH_PORT=8080
PRODUCT_PORT=8081
TRANSACTION_PORT=8082

UPLOAD_PATH=./uploads
LOG_PATH=./logs